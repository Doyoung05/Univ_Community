# 구현 노트

## 완료된 항목
- [x] **DB 스키마 확장:** `posts`에 `file_url` 추가, `comments`에 `is_accepted` 추가, `accept_answer` RPC 함수 정의 및 `archives` 스토리지 버킷 설정 마이그레이션 작성.
- [x] **자료실 & 과목 선택:** 게시글 작성 시 과목 선택 기능 및 자료실(`archive`) 타입 게시글에 대한 파일 업로드 기능 구현.
- [x] **파일 다운로드:** 게시글 상세 페이지에서 첨부 파일 다운로드 링크 제공.
- [x] **Q&A 채택 시스템:** Q&A 게시글 작성자가 댓글을 채택할 수 있는 기능 구현. 채택 시 RPC를 통해 포인트 지급 및 게시글 상태 변경 처리.
- [x] **관리자 페이지 구축:** `/admin` 경로에 대한 레이아웃 및 대시보드, 카테고리 관리, 과목 관리, 학사 일정 관리 페이지 구현.
- [x] **권한 제어:** 미들웨어를 통해 `/admin` 경로에 대한 관리자(`admin` role) 권한 체크 로직 추가.
- [x] **UI 개선:** 네비게이션 바에 관리자 링크 추가(관리자 계정 로그인 시), 게시글 목록 및 상세 페이지에서 과목명 및 해결 상태 표시.

## 계획 외 변경
- `src/components/board/AcceptButton.tsx`: Q&A 채택 로직을 별도 클라이언트 컴포넌트로 분리하여 서버 컴포넌트인 상세 페이지에서 사용할 수 있도록 함.
- `src/components/layout/Navbar.tsx`: 관리자 권한 확인을 위해 프로필 정보를 가져오는 로직 추가.

## 미해결 항목
- 없음.

## Reviewer 확인 요청
- `supabase/migrations/20260520000000_updates.sql` 파일의 RPC 함수와 스토리지 정책이 의도대로 작동하는지 확인 부탁드립니다.
- `/admin` 경로의 미들웨어 보호 로직이 보안상 적절한지 검토 바랍니다.
